###

void insertRB(loc x, int v)
  { x :-> p ** RedBlack(p, color, pathLength, vals) }
  { (pathLength1 == pathLength || pathLength1 == (pathLength + 1)) && vals1 =i {v} + vals ; x :-> q ** RedBlack(q, color1, pathLength1, vals1) }
{
  ??
}

###

